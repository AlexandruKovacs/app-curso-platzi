{% if app.user %}
    <p>
        Comentar como <strong>{{ app.user.name }}</strong>
    </p>
    {{ form(form) }}
{% else %}
    <p>
        <a href="{{ path('app_login') }}">Iniciar sesión</a> para comentar
    </p>
{% endif %}
